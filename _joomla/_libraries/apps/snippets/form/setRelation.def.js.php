<?php
// SET RELATION DEFAULT
// Ações default para atribuir uma relação entre apps
?>
if(typeof id !== "null" && typeof id !== "undefined" && id != 0) {
	relationId.val(id);
	btnPrev.remove();
	btnNext.remove();
}
